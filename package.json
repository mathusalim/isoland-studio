{
  "name": "isoland",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:client": "pnpm --filter @isoland/game-client dev",
    "dev:server": "pnpm --filter @isoland/game-server dev",
    "dev:assets": "pnpm --filter @isoland/asset-server dev",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "~5.6.2"
  }
}
